> Cycle 46 supersedes the priorities and metric claims below. Start with the **Current decision** in
> [the program](universal_stop_program.md) and [2026-09-05 review](retrospective_2026-09-05.md).
> The shared invalid-lead boundary passed both reviews, 973 tests (19 skipped), and exact nominal-output
> comparison. Deployment is authorized and being performed; verify the device before claiming it complete.
> The whole-approach governor remains OFF. The frozen plant fails complete stops; the source-derived wheel
> filter explains the acceleration/speed mismatch. Next comfort work: coherent full-episode model validation.

# HANDOVER PROMPT -- stopping program (universal stop governor), 2026-09-04

You are taking over an ongoing engineering thread from another model. Your job is BOTH: (1) continue the
work, and (2) review the work already done with an adversarial eye and look for improvements. Do not assume the
previous model was right. Where you disagree, say so with evidence, record it in the docs, and act.

## 0. Ground rules (non-negotiable, from the user)
- Communicate in ASD-STE100 Simplified Technical English. Lead with the outcome. Keep exact technical names.
- Update `docs/stopping/universal_stop_program.md` and `docs/stopping/worklog.md` EVERY time the approach changes,
  mid-cycle included; commit the docs with the change they describe. Retrospectives get their own dated file.
- Delete, don't patch: a legacy lane that misbehaves under the governor is deleted or absorbed, never guarded.
  The user explicitly rejected "building that huge tree of ifs again".
- Stopping code: mission-critical (wire-affecting) code you write yourself; large non-wire implementations
  may be delegated to sol (`gpt-5.6-sol`, `--effort xhigh`) through `~/.claude/scripts/codex-review.py` (never
  `codex-companion.mjs` directly). Every stopping design change gets a sol xhigh red-team BEFORE code. Every
  shipped commit gets an adversarial review through the same wrapper; TWO rounds maximum -- after that you fix
  what is real and sign off yourself, saying so.
- Rest-gap rule (user): aim 4-5 m, 3-4 comfort allowance, 3.0 hard floor, 5-6 wasted, never CHASE a position
  target at the end (aim early).
- Bar (user): "super smooth, above human", felt jerk <= 0.8 for EVERY stop class. Rests are SOLVED; the pump
  class is GONE; the remaining complaint is "brakes gently, then unnecessarily harshly before the stop".
- Never end a turn waiting: poll background jobs to completion or arm a waker; a dead job is a decision point.
- Deploy: `ssh -tt comma 'cd /data/openpilot && ./fullupdate.sh'`, verify the device hash; the device fetches
  over SSH with a read-only deploy key (see AGENTS.md "Device Update Workflow"; bundle fallback documented).
  A cereal schema change forces a ~15 min scons rebuild on the device: openpilot is up when `manager.py` runs
  (controlsd/plannerd only run with ignition on). Tests: `source .venv/bin/activate; : > /tmp/op_empty_pytest.ini;
  PYTHONDONTWRITEBYTECODE=1 python -m pytest selfdrive/controls/lib/tests/ selfdrive/controls/tests/test_longitudinal_planner.py
  selfdrive/controls/tests/test_santa_fe_stopping_lead_roll_in.py -q -c /tmp/op_empty_pytest.ini` (857+ pass, 19 skip).

## 1. Read these first (in this order)
1. `docs/stopping/retrospective_2026-09-02.md` -- why the process changed (metrics vs perception; complexity).
2. `docs/stopping/universal_stop_program.md` -- the program; read at least the entries from 2026-08-29 onward:
   the governor flip, the no-lead governor design, the attributed-safety step (design + shipped shadow + R1/R2
   changes), the 2026-09-04 mechanism finding, and the WHOLE-APPROACH GOVERNOR adopted design.
3. `docs/stopping/worklog.md` -- cycles 38-41 (bottom of the file).
4. `docs/stopping/review_cursor.json` -- last reviewed route counter (00002074) and per-cycle findings.
5. Code: `selfdrive/controls/lib/stopping_service.py` (governor_demand, barrier_demand, predictive_lead_demand,
   the ATTRIBUTED-SAFETY SHADOW block in update()), `stop_context.py` (StopSignals incl. track_age_s),
   `stopping_telemetry.py` (settle_summary + attr_* counters + bounded ring), `stopping_flags.py`
   (SERVICE_APPROACH_LAW="governor", ATTRIBUTED_SAFETY="shadow", possibly WHOLE_APPROACH_GOVERNOR),
   `longitudinal_planner.py` (Santa Fe block ~1750-1900; get_model_stop_distance; publish block),
   `longcontrol.py` (_run_stopping_service; lead_a/lead2/fcw/model_stop_d plumbing), `cereal/log.capnp`
   (LongitudinalPlan @45-@47, maybe @48+).
6. Tools: `tools/stopping/review/` -- `stop_index.py` (THE cycle entry point: `python tools/stopping/review/stop_index.py
   --since <hex route>`; `--routes A,B` reprocesses), `deep_stop.py`, `felt_one.py`, `human_baseline.py`,
   `late_brake_census.py`, `track_win.py`, `test_stop_index.py`. Data: `~/.route_sync/data/media/0/realdata`
   (sync: `python tools/route_sync/refresh_routes.py --include-rlog --newest-first`), corpus:
   `~/.route_sync/corpus/stop_index.jsonl`.

## 2. State at handover (2026-09-04 ~21:00 CEST)
- Device: build `ec334a3c` (governor live in-band; attributed-safety SHADOW; roll-in phantom guard). Repo tip
  on `!my-fp-new` is docs-only ahead of it.
- IN FLIGHT: sol is implementing the WHOLE-APPROACH GOVERNOR SHADOW (planner-side, flag off|shadow, wire
  unchanged) -- run id `20260904-210024-exec`, prompt `/tmp/wag/impl.prompt` (may have evaporated: the same
  spec is the adopted design entry in the program doc). Check with
  `python3 ~/.claude/scripts/codex-review.py check 20260904-210024-exec`; output in
  `~/.claude/codex-reviews/20260904-210024-exec.out`. Its diff is UNCOMMITTED in the working tree (`git status`).
  YOUR FIRST TASK: review that diff line by line (see §4), run the battery, fix or send back once, then
  adversarial review (wrapper, `--base <tip hash>`), max two rounds, commit, deploy, docs, memory.
- Attributed-safety shadow flip gate: drive 1 tallied 34 settles, 17% of eligible frames plan-bound, 52% of
  binds unexplained; the gate needs >= 100 held-out stops over >= 5 routes (spec in the program doc). Each new
  drive: run the index, tally attr_* (the index prints `unexp/bnd/n` per settle), decide.
- The user owes two drives: (a) a RATED drive -- bookmark once = "not perfectly smooth", twice = harsh, on
  every stop; used to calibrate a perception metric (the current `felt` scores only the last ~30 cm and already
  rates engaged stops 2x smoother than the driver's own); (b) the IDENTIFICATION drive (protocol v1 in the
  program doc) -- now a hard prerequisite for any LIVE flip of the whole-approach governor (plant model p90
  aEgo error <= 0.20, rest error <= 0.35 m).

## 3. Continue: the plan in order
1. Land the whole-approach SHADOW (above). Then the offline "before SHADOW" gate from the adopted design (replay
   >= 50 stopped-lead stops; flag-off equality; zero commits on disengaged/manual/force-coast/no-lead/crossing/
   phantom frames; >= 90% of hot stops move demand earlier) -- build the replay if the harness lacks it.
2. Each new drive: sync, index (`--since`), read the bookmarks first, tally both shadow gates, run
   `late_brake_census.py` and `human_baseline.py`; record findings; NO guard patches.
3. When the attributed-safety gate is met: implement "live" for the attributed-safety step (a_plan out of the
   min inside governor ownership), red-team first, two-round review, deploy, then delete the lanes listed as
   wire-dead in the program doc (roll-in floor + its oncoming/latch state first).
4. After the identification drive: validate the plant, run the "before LIVE" counterfactual gate for the
   whole-approach governor, flip it live, then the head-band deletion list (program doc, 2026-09-04 entry).
5. The no-lead / force-coast governor (design adopted 2026-08-29, first ship = force-coast class, shadow first).
6. The ML controller only after a perception-calibrated objective and a validated plant exist.

## 4. Review: attack these decisions (be specific, cite code/data; record verdicts in the program doc)
A. THE COMFORT CURVE. A(q) = 0.6 + 0.6*q^2/(q^2+2.5^2) (45 m of braking from 8 m/s). Is 0.6 at the tail still
   right given the terminal window's own descent (RAMP/HOLD) and the seam at 2.5 m/s? Is the 2.5 m/s blend
   width arbitrary? Would the user's "gentle" be better served by a constant-jerk onset + constant-decel body?
   Check with the human baseline (manual stops: whole-approach jerk p50 3.39, engaged 1.80).
B. THE HANDOFF. The service's governor pursues its curve with TAU 0.80 and demands -1.1 p50 / -2.0 p90 at entry
   because the arrival is hot. Once the whole-approach law owns the head band the arrival should match; but is
   the in-band pursuit gain still right for the RESIDUAL error? Consider an entry slew or a larger TAU on the
   error term only (offline replay on the 51 recorded entries: `~/.route_sync/corpus/stop_index.jsonl` gov_trace).
C. THE TERMINAL MACHINERY ignoring remaining distance (the 7 m bookmark: at 0.78 m/s with 3.5 m to go the
   creep-floor/RAMP finished the stop). "Never chase" is the user's rule for the END; is there a bounded,
   non-chasing way to keep the governor's +A_UP toward the anchor when the deficit is > 1.5 m and v is tiny?
   Evidence needed before touching it (it interacts with the leapfrog/over-brake history in memory).
D. ATTRIBUTED SAFETY: a_pred (RSS-style, adverse B_LEAD_MAX 2.5, rho = 0.45 with measured a_ego) -- is it too
   adverse (would it bind constantly behind crawling leads and become the new comfort law)? The shadow's
   attr_pred_bound counter answers this from rlogs; check it before the live step. Also: FCW is an eligibility
   veto rather than a demand (deviation from the red-team spec) -- right call?
E. TRUST-IN: identity-less (vision-only) leads never mature -> the attributed step never owns them; how common
   are they in the corpus (radarTrackId -1 while status)? If common, the step's coverage is small.
F. THE METRIC PROBLEM: `felt` (terminal window only) is the acceptance metric on paper. Propose the perception
   metric to calibrate against the rated drive (candidates: wheel-stop decel, late-brake peak in 0.45-3 m/s,
   approach jerk, pitch rate, deficit vs the comfort curve); pre-register the analysis so it is not a fishing
   expedition.
G. PROCESS: cycles 38-41 used ~2 review rounds per shipped fix and one sol red-team per design; complexity
   (planner constants 129->132) is still not falling. Is the delete-don't-patch rule being honoured? The roll-in
   phantom guard (3 rounds, +2 constants, +2 state vars) was the counter-example; the whole-approach deletion
   list is the remedy -- check that every new step comes with its deletion list and that deletions actually
   happen.
H. TOOLING: the stop index only itemises candidate segments; `NO_SERVICE_EVENT` was a matcher artefact until
   2026-09-02; `felt_appr` uses raw aEgo and spikes to 10+ on some stops (sensor/step artefact?) -- decide
   whether to smooth or to use pitch rate. Verify the corpus jsonl has no duplicate rows after `--routes`
   reprocessing.
I. SAFETY SWEEP of everything shipped since 2026-08-29 (governor flip 11ff4969, roll-in guard 59519208,
   attributed shadow 550b3446/06b?/d481117c): look for any path where telemetry or shadow code can reach the
   wire, any fail-open on non-finite inputs, and any change to takeover/handback seeding.

## 5. How to report
Lead with the outcome. Put the review verdicts (A-I) in the program doc as a dated entry, the actions in the
worklog, the cursor in review_cursor.json, and a condensed cycle record in the memory file
`~/.claude/projects/-Users-radoslawchybicki-Repos-openpilot-rch/memory/stopping-redesign-review.md` (prepend after
the "## Cycle chain" marker line; one-line pointer in MEMORY.md). Ask the user only for decisions that are
theirs (the two drives; any change to the rest-gap rule or the felt bar).
